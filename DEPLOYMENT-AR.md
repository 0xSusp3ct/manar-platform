# منار | متطلبات التشغيل وطريقة النشر

الإصدار 2.1 - التصميم المعتمد ب - 13 سبتمبر 2026

هذا الدليل مخصص لمالك المنصة ومسؤول الرفع. الملفات جاهزة للنشر؛ لا يعني تسليمها أن الدومين أو الاستضافة أو البريد قد فُعّلت بالفعل.

## 1. المطلوب من مالك الموقع

- حساب استضافة باسم الجهة يدعم Node.js 24.x وقرصًا دائمًا، وصلاحية للمسؤول الذي سيرفع الموقع.
- دومين وصلاحية تعديل DNS، أو البدء برابط الاستضافة المؤقت.
- بريد مالك لوحة الإدارة وكلمة مرور فريدة لا تقل عن 12 حرفًا؛ يفضل 16 حرفًا فأكثر.
- حساب GitHub للشفرة، إذا اختير النشر عبر GitHub. يفضل مستودع خاص Private.
- اختياري: حساب Resend ونطاق بريد موثّق ومفتاح إرسال. بدونه تعمل المنصة وتُشارك الرموز والروابط يدويًا.
- اعتماد موقع استضافة بيانات الجهات وسياسة النسخ الاحتياطي والصلاحيات قبل جمع بيانات حقيقية. رسوم الاستضافة والدومين والبريد منفصلة.

الدومين هو عنوان الموقع، والاستضافة تشغله، وGitHub يحفظ ملفات الشفرة. المسار المقترح: مستودع GitHub خاص مع Render Web Service وقرص دائم. ويمكن استخدام خادم VPS بدل Render.

GitHub Pages خدمة صفحات ثابتة؛ لذلك لا يشغّل هذا التطبيق الذي يحتاج خادمًا ومصادقة وقاعدة بيانات. كذلك لا يكفي رفع مجلد public وحده أو فتح index.html بالنقر المزدوج.

مرجع: [طبيعة GitHub Pages](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages).

## 2. رفع الملفات إلى GitHub

أنشئ مستودعًا خاصًا فارغًا دون README جديد. فك ZIP ثم افتح الطرفية داخل المجلد الذي يحتوي package.json وserver.js. استبدل OWNER وREPOSITORY بحسابك واسم المستودع ثم نفّذ:

```sh
git init
git add .
git commit -m "Release Manar 2.1"
git branch -M main
git remote add origin https://github.com/OWNER/REPOSITORY.git
git push -u origin main
```

يلزم Git وتسجيل الدخول إلى حسابك. لا تفعّل GitHub Pages. لا ترفع .env أو data أو النسخ الاحتياطية؛ ملف .gitignore يستبعدها. ارفع المجلدات والملفات المخفية المرفقة أيضًا، وبخاصة .env.example وملفات القفل.

## 3. التشغيل على Render

من Render اختر New ثم Web Service واربط المستودع. استخدم الإعدادات التالية:

| الإعداد | القيمة |
| --- | --- |
| Language / Runtime | Node |
| Branch | main |
| Root Directory | اتركه فارغًا إذا كانت ملفات المشروع في جذر المستودع |
| Build Command | npx --yes pnpm@11.19.0 install --frozen-lockfile --prod |
| Start Command | node server.js |
| Health Check Path | /api/health |
| NODE_VERSION | 24 |

اختر خدمة مدفوعة تدعم Persistent Disk، وأضف القرص قبل الاستخدام بمسار /var/data. اضبط DATABASE_PATH=/var/data/manar.db. اختر حجمًا مناسبًا وراقب استهلاكه. شغّل نسخة تطبيق واحدة فقط، ولا تستخدم Static Site أو نظام ملفات مؤقتًا لحفظ التقييمات. ينشئ التطبيق الجداول عند أول تشغيل، ولا توجد خطوة build للواجهة.

أضف متغيرات القسم التالي قبل أول تشغيل. يقرأ الخادم PORT الذي توفره Render؛ لا يلزم تغييره. بعدها نفّذ Deploy وافتح رابط الخدمة. قد يسبب نشر تحديث مع قرص دائم توقفًا قصيرًا؛ رتّب التحديثات خارج وقت التقييم.

المراجع: [نشر Node على Render](https://render.com/docs/deploy-node-express-app)، [القرص الدائم](https://render.com/docs/disks)، [تحديد Node](https://render.com/docs/node-version).

## 4. متغيرات البيئة للإنتاج

تُدخل في صفحة Environment لدى الاستضافة، وليس داخل ملفات الواجهة أو المستودع. القيم المكتوبة CAPITALS وexample.com أدناه أمثلة يجب استبدالها؛ لا تستخدمها كأسرار فعلية.

```dotenv
NODE_ENV=production
NODE_VERSION=24
BASE_URL=https://YOUR-SERVICE.onrender.com
DATABASE_PATH=/var/data/manar.db
COOKIE_SECURE=true
TRUST_PROXY=1
TOKEN_SECRET=PASTE_GENERATED_RANDOM_SECRET
ADMIN_EMAIL=owner@example.com
ADMIN_PASSWORD=SET_A_UNIQUE_STRONG_PASSWORD
SCORING_PROFILE=current50
RESEND_API_KEY=
RESEND_FROM=
```

لتوليد TOKEN_SECRET شغّل محليًا ثم انسخ الناتج إلى إعداد الاستضافة فقط:

```sh
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

- BASE_URL: الرابط العام الكامل بـ HTTPS دون شرطة مائلة في النهاية؛ استبدله بالدومين النهائي بعد ربطه.
- TOKEN_SECRET: احتفظ به ثابتًا وبنسخة آمنة. تغييره يعطل الرموز والروابط والجلسات القائمة؛ لا تولّد قيمة جديدة عند كل نشر.
- ADMIN_EMAIL وADMIN_PASSWORD: ينشئان حساب المالك عند أول تشغيل. تغيير كلمة المرور في المتغير لاحقًا لا يحدّث الحساب الموجود تلقائيًا.
- TRUST_PROXY=1: لإعداد خلف وسيط موثوق واحد. عند إضافة CDN أو تغيير مسار الشبكة يراجع مسؤول الخادم الضبط؛ لا تجعل منفذ التطبيق الداخلي متاحًا للجمهور في VPS.
- SCORING_PROFILE=current50: يحافظ على نسخة الموقع ذات 50 مؤشرًا. لا تتحول إلى guide48 إلا باعتماد مالك المقياس، وقبل جمع النتائج الحقيقية أو ضمن ترحيل مخطط.
- البريد اختياري: عند تفعيله أضف RESEND_API_KEY واجعل RESEND_FROM عنوانًا موثقًا، مثل Manar <reports@YOUR-DOMAIN>. أرسل رسالة اختبار قبل الاعتماد.

## 5. ربط الدومين والتحقق قبل الإطلاق

1. في Render افتح Settings ثم Custom Domains ثم Add Custom Domain. أدخل الدومين أو نطاقًا فرعيًا مثل manar.example.com.
2. لدى مزود الدومين، أضف سجلات DNS التي تعرضها Render لذلك النطاق تحديدًا. لا تخمّن عنوان IP ولا تغيّر سجلات البريد MX أو سجلات الخدمات الأخرى.
3. ارجع إلى Render واضغط Verify، وانتظر تحقق النطاق وصدور شهادة HTTPS.
4. عدّل BASE_URL إلى https://manar.example.com دون شرطة أخيرة، وأعد نشر الخدمة. اختبر استخدام الدومين نفسه في الجلسة كاملة.
5. احتفظ بالنطاق القديم إن كانت عليه روابط تقارير سبق توزيعها، أو هيئ إعادة توجيه تحفظ المسار كاملًا. لا تبدأ بنشر روابط تجريبية للجهات.

مرجع: [ربط الدومين وHTTPS لدى Render](https://render.com/docs/custom-domains).

قائمة الاستلام بعد النشر:

- افتح /api/health وتأكد من ظهور {"ok":true}، وافتح الصفحة الرئيسية ولوحة /admin.
- سجل دخول المالك، وأنشئ طلبًا تجريبيًا، واعتمده ثم افتح التقييم بالرمز الصادر.
- أجب عن بعض المؤشرات، وانتظر «تم الحفظ»، وحدّث الصفحة للتحقق من الاستئناف.
- أكمل التقييم وتأكد من النتائج ورابط التقرير الخاص، وافتحه في نافذة خاصة دون حساب إدارة.
- اختبر رابط المشاركة وPDF والطباعة على A4 من الكمبيوتر والجوال. عطّل ترويسة المتصفح وتذييله في الطباعة.
- اختبر التوصيات وتصدير Excel وصلاحيات المشرف، والبريد إذا فُعّل.
- أعد تشغيل خدمة الاختبار: يجب بقاء النتيجة والرابط صالحين. هذا يتحقق من القرص الدائم وثبات المفتاح.
- لا تجمع بيانات فعلية قبل نجاح هذه الخطوات. الاختبار المحلي لا يغني عن اختبار الاستضافة والبريد والدومين الحقيقيين.

النسخ الاحتياطي: نسخة يومية متسقة عبر أداة نسخ SQLite، محفوظة مشفرة خارج قرص التشغيل، مع اختبار استعادة دوري. لا تنسخ manar.db منفردًا أثناء تشغيله؛ استخدم آلية SQLite أو أوقف التطبيق في نافذة صيانة وانسخ مجلد البيانات كاملًا. احفظ TOKEN_SECRET وإعدادات التشغيل منفصلة بأمان. قبل كل تحديث، خذ نسخة احتياطية واحتفظ بإصدار الشفرة السابق. لا تحذف القرص أو تستبدل قاعدة البيانات عند تحديث الملفات.

## 6. بديل Render: خادم VPS مع Docker

لمسؤول الخادم: يلزم خادم Linux مع Docker وCaddy أو Nginx وقرص دائم ونسخ احتياطي. يشغّل هذا البديل التطبيق نفسه؛ لا يلزم شراء Render معه. تعليمات Docker التالية لإعداد جديد فقط وليست أمرًا لاستبدال تشغيل قائم.

ارفع الملفات كاملة إلى مجلد مستقل مثل /opt/manar. انسخ .env.example إلى .env واملأ الأسرار وبيانات المالك كما سبق، ثم استخدم إعدادات الإنتاج مع التعديلات التالية:

```dotenv
BASE_URL=https://manar.example.com
PORT=3000
DATABASE_PATH=/app/data/manar.db
COOKIE_SECURE=true
TRUST_PROXY=1
```

من مجلد المشروع نفّذ الأوامر التالية في طرفية Linux؛ docker run أمر واحد ولو عُرض على أكثر من سطر:

```sh
chmod 600 .env
docker build -t manar:2.1 .
docker volume create manar-data
docker run -d --name manar --restart unless-stopped --env-file .env -p 127.0.0.1:3000:3000 -v manar-data:/app/data manar:2.1
```

المجلد /app/data يجب أن يكون قابلًا للكتابة لمستخدم node داخل الحاوية. لا تحذف volume باسم manar-data عند تحديث التطبيق.

اربط DNS الخاص بنطاق الموقع بعنوان الخادم، وثبّت Caddy كخدمة على الخادم نفسه. أضف كتلة النطاق أدناه إلى إعداد Caddy الصحيح، دون استبدال إعدادات مواقع أخرى:

```caddy
manar.example.com {
    reverse_proxy 127.0.0.1:3000
}
```

استبدل اسم النطاق. تحقق من صحة الإعداد ثم أعد تحميل Caddy. اسمح بالمنفذين 80 و443 للوصول العام؛ يبقى 3000 داخليًا. يتيح Caddy إصدار وتجديد HTTPS تلقائيًا عند صحة DNS وإمكانية الوصول. طبّق قائمة التحقق والنسخ الاحتياطي نفسها. هذا المسار يحتاج مسؤول خادم؛ لم تُختبر حاوية Docker على خادم العميل ضمن هذا التسليم.

مرجع: [متطلبات HTTPS التلقائي في Caddy](https://caddyserver.com/docs/automatic-https).

للتجربة المحلية فقط: اتبع README.md؛ يستخدم HTTP مع NODE_ENV=development وCOOKIE_SECURE=false وTRUST_PROXY=0. لا تنقل هذه القيم إلى الإنتاج. الأوامر قابلة للنسخ من هذا الملف النصي داخل الحزمة.
